const router = require('express').Router();
const { User, Workout, WorkoutHistory } = require('../models');
const withAuth = require('../utils/auth');

// TODO: Add the homepage route here (So when you go to localhost:3)



// TODO: Add the login route here




// TODO: Add the signup route here



